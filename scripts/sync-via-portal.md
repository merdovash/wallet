# Sync this service via portal orchestrator (on VPS where portal is checked out).
# Example:
#   cd /var/www/portal && node scripts/sync-services.mjs --only wallet
#
# Requires portal config/services.yaml entry id: wallet
