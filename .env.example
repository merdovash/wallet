# Same Postgres as trip_budget / portal — shared users, sessions, FX cache.
DATABASE_URL=postgresql://finance:finance@localhost:5432/finance

# SSO: parent cookie domain (production) + portal apex (disables local login/register API)
# COOKIE_DOMAIN=.example.com
# PORTAL_URL=https://example.com
# VITE_PORTAL_URL=https://example.com
